<Window x:Class="Laplace.Desktop.ExtractArchiveDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Extract Archive" Height="300" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="ArchiveInput" materialDesign:HintAssist.Hint="Archive Path" Style="{StaticResource MaterialDesignFloatingHintTextBox}" IsReadOnly="True" Margin="0,0,0,16" />
        
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="DestinationInput" materialDesign:HintAssist.Hint="Extract to" Style="{StaticResource MaterialDesignFloatingHintTextBox}" />
            <Button Content="..." Grid.Column="1" Click="Browse_Click" Margin="8,0,0,0" Width="40" VerticalAlignment="Bottom" />
        </Grid>

        <PasswordBox x:Name="PasswordInput" materialDesign:HintAssist.Hint="Password (Optional)" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Grid.Row="2" Margin="0,0,0,16" />
        
        <CheckBox x:Name="OverwriteCheck" Content="Overwrite existing files" Grid.Row="3" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="5" Margin="0,16,0,0">
            <Button Content="Extract" IsDefault="True" Click="Ok_Click" Margin="0,0,8,0" Width="100" />
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80" />
        </StackPanel>
    </Grid>
</Window>
