<Window x:Class="Laplace.Desktop.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Password Required" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderTextBlock" Text="Archive is encrypted" FontWeight="Bold" FontSize="16" Margin="0,0,0,8" />
        <TextBlock x:Name="DescriptionTextBlock" Text="Please enter the password for the archive:" TextWrapping="Wrap" Margin="0,0,0,16" Grid.Row="1" />
        <PasswordBox x:Name="PasswordInput" materialDesign:HintAssist.Hint="Password" Style="{StaticResource MaterialDesignFloatingHintPasswordBox}" Grid.Row="2" />
        
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,16,0,0">
            <Button Content="OK" IsDefault="True" Click="Ok_Click" Margin="0,0,8,0" Width="80" />
            <Button Content="Cancel" IsCancel="True" Style="{StaticResource MaterialDesignOutlinedButton}" Width="80" />
        </StackPanel>
    </Grid>
</Window>
